# This is the main Samba configuration file. You should read the
# smb.conf(5) manual page in order to understand the options listed
# here. Samba has a huge number of configurable options (perhaps too
# many!) most of which are not shown in this example
#
# For a step to step guide on installing, configuring and using samba, 
# read the Samba-HOWTO-Collection. This may be obtained from:
#  http://www.samba.org/samba/docs/Samba-HOWTO-Collection.pdf
#
# Many working examples of smb.conf files can be found in the 
# Samba-Guide which is generated daily and can be downloaded from: 
#  http://www.samba.org/samba/docs/Samba-Guide.pdf
#
# Any line which starts with a ; (semi-colon) or a # (hash) 
# is a comment and is ignored. In this example we will use a #
# for commentry and a ; for parts of the config file that you
# may wish to enable
#
# NOTE: Whenever you modify this file you should run the command "testparm"
# to check that you have not made any basic syntactic errors. 
#

#(https://www.samba.org/samba/docs/current/man-html/smb.conf.5.html)
#======================= Global Settings =====================================
[global]
   workgroup = {{ configuration.workgroup }}
   server string = {{ configuration.name }}

   server role = standalone server

   {% if hosts_allow %}hosts allow = {{ configuration.hosts_allow }}{% endif %}
   {% if hosts_deny %}hosts deny = {{ configuration.hosts_deny }}{% endif %}

   max log size = 50
   dns proxy = no 

   {% if ports %}smb ports = {{ configuration.ports }}{% endif %}

#============================ Share Definitions ==============================

{% for s in configuration.shares %}
[{{ s.name }}]
   {% if s.remote_path %}path = {{ s.remote_path }}{% endif %}
   {% if s.comment %}comment = {{ s.comment }}{% endif %}
   {% if s.guest_ok %}guest ok = yes{% endif %}
   {% if s.browseable_no %}browseable = no{% endif %}
   {% if s.writable %}writable = yes{% endif %}
   {% if s.user %}valid users = {{ s.user.name }}{% endif %}
{% endfor %}
