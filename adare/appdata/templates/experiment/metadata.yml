# Experiment metadata
name: "{{ experiment }}"
description: "Brief description of what this experiment does"

# Specify which environments this experiment can run on
environments:
  # Add environment names here, e.g.:
  # - win11
  # - ubuntu22

# For advanced metadata options, see:
# https://docs.adare.example.com/experiments/metadata-reference
