from pathlib import Path
import sys

from guiautomation.Scenario.Scenario import Scenario
from guiautomation.run import run
from guibot.guibot import GuiBot

import logging
log = logging.getLogger(__name__)


class {{ name }}(Scenario):
    description = 'place the description of your scenario here '

    def __init__(self):
        """
            initialization function which in most cases should not be changed (except there is a need to use a different display controller or computer vision backend for guibot)
        """
        super().__init__()
    pass

    def prepare(self):
        """
            this function can be used to execute some commands before the clicks happen (e.g. creating a file)
        """
        pass

    def run(self):
        """
            this function should be used to execute the gui automation steps
        """
        log.info(f'scenario {type(self).__name__} started')

        # place the code to execute some stuff here


        log.info("scenario " + type(self).__name__ + " done")

if __name__ == '__main__':
    if len(sys.argv) < 2:
        print('missing file for config path')
        exit(-1)
    run({{ name }}, config_file=Path(sys.argv[1]))